BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TblMSContractMain] ADD [TradeMonth] DATETIME2 CONSTRAINT [TblMSContractMain_TradeMonth_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
